import 'package:flutter/material.dart';

class ProfileWidgetsTree extends StatelessWidget {
  const ProfileWidgetsTree({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Profile"));
  }
}
