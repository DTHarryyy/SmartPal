import 'package:flutter/material.dart';

class AppColor {
  // Brand Colors (Modern Blue Theme)
  static const primary = Color(0xFF4A90E2); // modern soft blue
  static const secondary = Color(0xFF50E3C2); // teal accent

  // Neutrals (modern gray scale)
  static const black = Color(0xFF1A1A1A); // deep neutral black
  static const white = Color(0xFFFFFFFF); // pure white
  static const gray = Color.fromARGB(255, 211, 211, 212); // iOS gray
  static const lightGray = Color(0xFFF2F2F7); // modern soft background

  // Status Colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFC107);
  static const error = Color(0xFFE53935);
}
