import 'package:flutter/material.dart';

class CustomOnLoading extends StatelessWidget {
  const CustomOnLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return CircularProgressIndicator();
  }
}
