import 'package:flutter/material.dart';

class StudyGroup extends StatelessWidget {
  const StudyGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('Study group')),
    );
  }
}
