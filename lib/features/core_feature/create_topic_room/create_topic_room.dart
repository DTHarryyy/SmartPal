import 'package:flutter/material.dart';

class CreateTopicRoom extends StatelessWidget {
  const CreateTopicRoom({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('TopicRoom')),
    );
  }
}
