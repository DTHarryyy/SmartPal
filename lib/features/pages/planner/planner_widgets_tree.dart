import 'package:flutter/material.dart';

class PlannerWidgetsTree extends StatelessWidget {
  const PlannerWidgetsTree({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Planner"));
  }
}
